  # - name: C
  #   become: true  
  #   # hosts: "{{COMPONENT}}"
  #   hosts: all
  #   roles:
  #     - role: "{{COMPONENT}}"

  # --- 

  - name: Configuging Expense App 
    hosts: all
    become: true
    roles:
      - "{{ COMPONENT }}"